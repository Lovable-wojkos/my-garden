SUPABASE_URL=###
SUPABASE_ANON_KEY=###

# Magic-link callback origin — must match Supabase Auth → URL Configuration (hosted) or site_url (local).
# Local dev with npm run dev: http://localhost:4321
SITE_URL=http://localhost:4321

# E2E tests (Playwright) — magic-link auth via service role
E2E_EMAIL=###
SUPABASE_SERVICE_ROLE_KEY=###

# Cron weather sync (local integration tests + Vercel cron)
CRON_SECRET=###
